{
  "name": "lambda-aws-checkip",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "dependencies": {
    "aws-sdk": "^2.711.0",
    "bluebird": "^3.7.2",
    "lodash": "^4.17.21",
    "request-ip": "^2.1.3"
  },
  "devDependencies": {},
  "scripts": {
    "zip": "rm -rf outputs/lambda.zip && mkdir -p outputs && zip -qq -9 -r outputs/lambda.zip index.js node_modules package.json package-lock.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
